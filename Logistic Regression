from sklearn.linear_model import LogisticRegression
import numpy as np

X = np.array([[1], [2], [3], [4], [5], [6]])
y = np.array([0, 0, 0, 1, 1, 1]) # Class 0 or 1

model = LogisticRegression().fit(X, y)
print(f"Class for 4.5: {model.predict([[4.5]])}")

output: Class for 4.5: [1]
