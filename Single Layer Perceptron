from sklearn.linear_model import Perceptron

X = [[0,0], [0,1], [1,0], [1,1]]
y = [0, 0, 0, 1] # AND logic

model = Perceptron().fit(X, y)
print(f"Perceptron Predictions: {model.predict(X)}")

  output: Perceptron Predictions: [0 0 0 1]
  
